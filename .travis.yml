language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-milkshake' ]; then cd .. && eval "mv $currentfolder generator-milkshake" && cd generator-milkshake; fi

